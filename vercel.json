{
  "buildCommand": "cd the_mind && ../flutter/bin/flutter pub get && ../flutter/bin/flutter pub run build_runner build --delete-conflicting-outputs && ../flutter/bin/flutter build web --release",
  "outputDirectory": "the_mind/build/web",
  "framework": null,
  "installCommand": "git clone https://github.com/flutter/flutter.git -b stable --depth 1 && flutter/bin/flutter config --enable-web && flutter/bin/flutter precache --web",
  "devCommand": "cd the_mind && flutter run -d web-server --web-hostname=0.0.0.0 --web-port=3000",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
